{

// Build the directory
	
 	TString xeDir(gSystem->Getenv("XEPHYR_DIR"));
	gSystem->Exec("mkdir -p " + xeDir );
 	TString inputDir = xeDir + "SR1/StatisticalAnalyses/inputs_for_likelihood/"+ LAX + "/sliced_2d_templates/";

// Production of the models
	
	// Background
	TH2F backgr


}
